# Copy this file to .env.local for local development
# Set this in Netlify Dashboard → Site Settings → Environment Variables for production

# Your PHP backend URL (where your backend is hosted)
# Local development: leave empty (uses Vite proxy)
# Production (Netlify): set to your backend host URL
VITE_API_URL=https://your-backend-url.com/backend/api